[project]
name = "fastapi-programs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "'VidhyananthNatarajan'", email = "'vidhyananthsn59@gmail.com'" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "uvicorn>=0.52.4",
]

[project.scripts]
fastapi-programs = "fastapi_programs:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
